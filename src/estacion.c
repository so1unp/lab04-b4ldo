#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    (void)argc; (void)argv;
    // Agregar código aquí.

    // Termina la ejecución del programa.
    exit(EXIT_SUCCESS);
}
